//slice image
//drawImage(image, sx, sy, sWidth, sHeight, dx, dy, dWidth, dHeight)

// randomize position of tiles
// save original loaction of tiles

// allow click for the image to move
// each click moves the tile in a single direction